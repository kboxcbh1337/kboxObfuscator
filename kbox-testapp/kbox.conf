# KBox protection config for the testapp.
# Renaming + string encryption + control flow + resource obfuscation.
renameIdentifiers = true
encryptStrings = true
obfuscateControlFlow = true
controlFlowStrength = 2
stringEncryptionStrength = 1

# Resource (non-class file) obfuscation: rename + encrypt.
obfuscateResources = true
fixKotlinMetadata = true
# Encrypt the sensitive config file (AES-GCM).
encryptResource = config/*.properties
# Exclude plaintext + signature files from renaming entirely.
excludeResource = assets/banner.txt, META-INF/*.SF, META-INF/*.RSA, META-INF/*.DSA

# Anti-analysis: anti-decompiler, anti-debug, watermark, integrity check.
antiDecompilerLevel = 1
antiDebug = true
integrityCheck = true
watermark = KBOX-TEST-2026-CUSTOMER-001
exceptionJumpObf = true

# Keep the public entry-point class so its main(String[]) stays callable.
keepPrefix = com.kbox.testapp.Main
# Keep Greeter.greet (public API used by reflection-free callers).
keepMember = com.kbox.testapp.Greeter#greet(Ljava/lang/String;)Ljava/lang/String;

# CC path on this Windows machine.
cc = C:/msys64/mingw64/bin/gcc.exe
